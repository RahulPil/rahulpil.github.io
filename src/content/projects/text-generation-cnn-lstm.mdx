---
title: "CNN-LSTM text generation"
summary: "A hybrid convolutional-recurrent language model in PyTorch, trained on Reddit comments to generate short text."
date: 2023-03-01
tags: ["pytorch", "nlp", "text-generation"]
stack: ["PyTorch", "NumPy"]
links: {}
---

A hybrid CNN-LSTM language model built in PyTorch, trained on a dataset of Reddit comments
to generate short, plausible text. The CNN front-end captures local n-gram patterns; the
LSTM tail models longer-range dependencies.

_This is a stub — expand with more detail when you have time._
